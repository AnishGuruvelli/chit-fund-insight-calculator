# Copy to android/keystore.properties (gitignored) and fill in your Play signing credentials.
# Place your upload keystore at the path below (default: android/app/release-key.keystore).

storeFile=app/release-key.keystore
storePassword=YOUR_KEYSTORE_PASSWORD
keyAlias=YOUR_KEY_ALIAS
keyPassword=YOUR_KEY_PASSWORD
